<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.5</version>
    <relativePath/>
  </parent>
  <groupId>com.actiongate</groupId>
  <artifactId>actiongate-parent</artifactId>
  <version>0.2.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>ActionGate</name>
  <description>Agent action release gate and recoverable execution platform</description>
  <properties>
    <java.version>21</java.version>
    <json-schema-validator.version>1.5.9</json-schema-validator.version>
    <temporal.version>1.38.0</temporal.version>
  </properties>
  <modules>
    <module>libs/contract-core</module>
    <module>libs/policy-contract</module>
    <module>libs/trace-contract</module>
    <module>libs/workflow-contract</module>
    <module>apps/worker</module>
    <module>apps/control-plane</module>
  </modules>
  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>com.actiongate</groupId>
        <artifactId>workflow-contract</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>io.temporal</groupId>
        <artifactId>temporal-sdk</artifactId>
        <version>${temporal.version}</version>
      </dependency>
      <dependency>
        <groupId>io.temporal</groupId>
        <artifactId>temporal-testing</artifactId>
        <version>${temporal.version}</version>
      </dependency>
      <dependency>
        <groupId>com.actiongate</groupId>
        <artifactId>contract-core</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.actiongate</groupId>
        <artifactId>policy-contract</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.actiongate</groupId>
        <artifactId>trace-contract</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.networknt</groupId>
        <artifactId>json-schema-validator</artifactId>
        <version>${json-schema-validator.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>
</project>
